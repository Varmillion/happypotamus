<br>
<%= form_for([@pin, @pin.comments.build]) do |f| %>
  <div class="container">
    <div class="col-md-4">
    <%= f.text_field :content, :placeholder => "Press enter to comment", size: "15x1", :class => 'text2' %>
    </div>
  </div>
<% end %>