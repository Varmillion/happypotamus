<%= form_for([@pin, @comment]) do |f| %>
  <div class="container">
    <div class="col-md-4">
    <%= f.text_area :content, :placeholder => "Edit comment", size: "15x1", :class => 'text2' %>
    <%= f.submit 'Edit', :class => 'submitButton btn btn-default' %>
    </div>
  </div>
<% end %>